import React from 'react';
import { BrowserRouter,Routes } from 'react-router-dom';

function App(props) {
    return (
        <BrowserRouter>
            <Routes>

            </Routes>
        </BrowserRouter>
    );
}

export default App;